OK. So these are the requirements for the autonomous coding agent, visible through a single p of glass. for tinder. And so here's the idea. and there are some things that we'll need to figure out, but the bigger concept is that there is a website, which is a single paint of glass that developers can go to, and they can see all of the agents that they have available, they can see the MCP servers, they can see the blueprints and then they can build deploy, they can play with. I'm not exactly sure what that means. Probably just like chat with invoke experiment with.. They can debug and they can monitor. So those are the requirements for the platform, the single paint of glass, you should be able to build an agent, you should be able to deploy an agent, you should be able to invoke, like chat with that agent, you should be able to monitor that agent, and then you should be able to debug the agent. And so that would be like probably evaluations, both on demand, or you could set up like a long-running one. So that's the requirements for the single p of glass. Now, what that sits on top of, it's kind of a bigger development stack, and this is a cloud based autonomous coding agent platform. And so the first step is that you will have someone that produces a one pager, and so this will be, you know, something that they come with, right? And then maybe they do some bid cobing or some vibe coding. They develop a prototype. That's kind of done. outside of the system. And then the system should be able to handle taking that as an input. and we can just start with like a simple, you know, here's here the documentuments or whatever the output is. Like, here it is, go get it. And that should kick off the full development life cycle. And so what that means is there's an intake process and that intake process analyzes the prototype, the VI cove, the one pager, and it develops a speck. That spec is converted into a Jira epic and with that epic we have individual tickets for all of the work streams. And these, I believe are going to be the blueprint workstams. And so each ticket is created with a workstream. There are workstreams such as the back end design, iOS design, Android design, security review, privacy review, analytics and localization. So those are all the ge tickets that are created under this epic. and that is the design phase. The output of the design phase is another set of JR tasks. And so these Jiratasks are all still linked to that Jira Eic. And each of those, now that they're fully specked out, each of those gets passed to the implementation autonomous coding agent. and they are each developed, so like the back end would be developed. The iOS would be developed. The android would be developed. Security, analytics, localization, whatever those have, those are all developed, and the end result would be a poll request for all of that work that was done. And so one concept that they like is leveraging the AI DLC framework and how that framework goes through all of the spec creation steps to create a thorough spec and then passes that to implementation, where the implementation and testing is done. So those are the requirements for this platform. Now, we are looking at leveraging this autonomous coating agent harness that has been open sourced by by AWS as part of this MVP that we are creating. And so the idea here is to build all of this out in an MVP. And so the main components that we need to develop are the front end. single pane of glass view into all of this. as well as the cloud based autonomous coding agent, as we have speced out.